# 新增功能说明 - 比货功能

## 功能概述

本次新增了"比货"功能，允许用户在商品详情页面同时查看两个商品的详细信息并进行对比。用户可以在商品列表页选择两个商品进行对比，然后在详情页面查看两者的详细规格差异，并选择其中一个进行购买。

## 主要功能点

### 1. 比货模式入口
- 在商品列表页面，新增"比货"按钮
- 点击"比货"按钮进入比货模式，可以勾选两个商品进行对比
- 再次点击"确认"按钮跳转到商品详情对比页面

### 2. 商品详情对比页面
- **价格对比**：同时显示两个商品的价格，第二个商品价格用灰色背景突出显示
- **标题对比**：同时显示两个商品的标题，第二个商品标题用灰色背景突出显示
- **详情规格对比**：
  - 第一件商品的每个详情段落下方，插入第二件商品的对应详情段落
  - 第二件商品的详情段落使用灰色背景（`background-color: #E5E7EB`）进行视觉区分
  - 规格信息包括：品牌型号、屏幕尺寸、处理器、摄像头配置、电池容量等

### 3. 底部操作按钮优化
- **比货模式下**：将原有的"客服"、"购物车"、"加入购物车"、"立即购买"四个按钮替换为：
  - **选一**：选择第一件商品进行购买（橙色按钮）
  - **选二**：选择第二件商品进行购买（红色按钮）
- **非比货模式下**：保持原有的四个按钮不变
- 两个按钮大小一致，各占一半宽度，使用圆角设计

### 4. 页面状态管理优化
- **从订单确认页返回详情页时**：自动隐藏"比货"按钮，避免重复进入比货模式
- **页面导航逻辑**：通过记录当前URL状态，智能控制比货按钮的显示/隐藏

## 技术实现

### 文件修改说明

#### 1. `entry/src/main/resources/rawfile/product_detail.html`
- 移除商品详情区域的硬编码内容，改为动态生成
- 商品详情区域使用`<div id="product-info"></div>`占位

#### 2. `entry/src/main/resources/rawfile/js/product_detail.js`
- **数据结构增强**：为每个商品添加`details`数组，包含5段规格信息
- **比货模式检测**：通过URL参数`compareIdx`判断是否进入比货模式
- **动态渲染**：
  - 比货模式下：交替渲染两个商品的详情段落
  - 非比货模式下：只显示当前商品的详情
- **底部按钮切换**：比货模式下动态生成"选一"和"选二"按钮
- **事件处理**：
  - `chooseOne()`：选择第一件商品，跳转订单确认
  - `chooseTwo()`：选择第二件商品，跳转订单确认

#### 3. `entry/src/main/resources/rawfile/style/product_detail.css`
- **新增样式类**：
  - `.detail-p`：详情段落基础样式
  - `.compare-detail`：对比商品的灰色背景样式
  - `.btn-compare`：比货按钮基础样式
  - `.btn-choose-one`：选一按钮橙色渐变
  - `.btn-choose-two`：选二按钮红色渐变

#### 4. `entry/src/main/ets/pages/IndexPage.ets`
- **状态管理**：新增`lastUrl`变量记录当前页面URL
- **比货按钮显示逻辑**：
  - `onPageShow()`：根据`lastUrl`判断是否显示比货按钮
  - `onPageBegin()`：记录当前URL，详情页隐藏比货按钮
- **页面跳转**：`jumpOrderConfirm()`方法跳转时隐藏比货按钮

### 数据结构示例

```javascript
let productList = [
  {
    'id': 1,
    'name': 'HW nove9 新品手机【官方标配】8+128G全网通',
    'img': 'imgs/ic_1.png',
    'price': 4488,
    'details': [
      '品牌：HW | 型号：nove9 | 上市时间：2024年',
      '屏幕尺寸：6.7英寸 OLED 曲面屏，支持120Hz高刷',
      '处理器：骁龙8 Gen2 八核 3.2GHz',
      '后置摄像头：5000万像素主摄 + 1300万超广角 + 1200万长焦',
      '电池容量：4800mAh，支持66W超级快充'
    ]
  },
  // ... 其他商品
];
```

## 界面效果

### 比货详情页布局

```
┌────────────────────────────────────┐
│  商品图片轮播区域                   │
├────────────────────────────────────┤
│  ¥4488      [¥4488]  ← 第二件商品价格（灰底）
│  HW nove9 新品手机...              │
│  [XX设备 新品优惠！...] ← 第二件商品标题（灰底）
│  商品详情：                         │
│  品牌：HW | 型号：nove9 | ...      │ ← 第一件商品
│  ┌──────────────────────────────┐ │
│  │ 品牌：XX | 型号：Pro Max | ...│ │ ← 第二件商品（灰底）
│  └──────────────────────────────┘ │
│  屏幕尺寸：6.7英寸 OLED 曲面屏...   │ ← 第一件商品
│  ┌──────────────────────────────┐ │
│  │ 屏幕尺寸：6.8英寸 LCD 直屏...│ │ ← 第二件商品（灰底）
│  └──────────────────────────────┘ │
│  ...（继续交替显示）               │
├────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ │
│  │    选 一     │ │    选 二     │ │
│  │   (橙色)     │ │   (红色)     │ │
│  └──────────────┘ └──────────────┘ │
└────────────────────────────────────┘
```

### 视觉样式
- **第二件商品信息**：灰色背景（`#E5E7EB`），圆角4px，内边距4px 8px
- **选一按钮**：橙色渐变（`#FF914C` → `#FEA748`）
- **选二按钮**：红色渐变（`#F64D43` → `#EB344D`）
- **按钮尺寸**：高度40px，圆角20px，等宽分布

## 使用流程

1. **进入比货模式**
   - 在商品列表页点击"比货"按钮
   - 勾选两个要对比的商品
   - 点击"确认"按钮

2. **查看商品对比**
   - 进入商品详情对比页面
   - 查看两个商品的详细规格对比
   - 每个规格段落下方都有对应商品的对比信息

3. **选择购买**
   - 点击"选一"：购买第一件商品
   - 点击"选二"：购买第二件商品
   - 跳转到订单确认页面

4. **返回逻辑**
   - 从订单确认页返回详情页：不显示"比货"按钮
   - 从详情页返回列表页：正常显示"比货"按钮

## 兼容性说明

- 保持原有非比货模式的功能完整性
- 所有修改均为增量更新，不影响原有业务流程
- 样式兼容原有设计规范
- 支持API级别：HarmonyOS 5.0.5 Release及以上

## 测试要点

1. **比货功能测试**
   - 正常进入/退出比货模式
   - 商品勾选状态正确传递
   - 详情页对比信息正确显示

2. **样式测试**
   - 灰色背景对比段落显示正常
   - 底部按钮样式和布局正确
   - 响应式布局适配

3. **交互测试**
   - "选一"/"选二"按钮点击跳转正确
   - 页面返回逻辑正确
   - 比货按钮显示/隐藏逻辑正确

## 注意事项

1. 比货模式仅支持两个商品的对比
2. 商品详情数据需要预先在`product_detail.js`中配置
3. 页面状态管理依赖URL参数传递
4. 从订单确认页返回时，比货按钮会自动隐藏，避免重复进入比货模式

---

**版本**: 1.0.0  
**更新日期**: 2025年1月  
**开发者**: HarmonyOS开发团队